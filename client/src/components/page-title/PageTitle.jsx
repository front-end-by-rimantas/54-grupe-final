export function PageTitle({ title }) {
    return (
        <div className="row">
            <div className="col-12">
                <h1 className="display-1">{title}</h1>
            </div>
        </div>
    );
}